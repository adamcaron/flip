version: '3.1'
services:
  api:
    image: node:alpine
    ports:
     - 8085:8085
    volumes:
     - ${API_MOUNT}:/api
    working_dir: /api
    entrypoint: sh
    command:
     - -c
     - |
       npm install
       exec node_modules/.bin/nodemon -L server.js
  ui:
    image: node:alpine
    ports:
     - 8080:8080
    volumes:
     - ${UI_MOUNT}:/ui
    working_dir: /ui
    entrypoint: sh
    command:
     - -c
     - |
       npm install
       exec node build/dev-server.js